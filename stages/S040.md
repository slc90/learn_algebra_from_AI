# S040 · 用有限生成控制无限过程

## 这一阶段在研究什么

有限生成条件把无限增添关系的过程限制住。Noether 性要求所有子对象都能有限描述，强于对象本身有限生成。

## 进入这个 Stage 之前

[S025 · 把“数乘”替换成一般环的作用](./S025.md)、[S039 · 允许某些元素可逆，会改变什么](./S039.md)

### 快速回顾与符号语言

需要环、理想与模。Noether 模指子模升链终止；环作为自身模 Noether 等价于理想升链终止。有限生成是有有限个生成元，有限表示还要求关系有限生成。

## 先看几个具体例子

### 例 1 · 最简单模型

Z 的每个理想为 nZ，所以 Noether；但下降链 (2)⊃(4)⊃… 不终止。

### 例 2 · 结构真正开始工作

k[x₁,x₂,…] 中 (x₁)⊊(x₁,x₂)⊊… 持续增长。

### 例 3 · 失败与边界

任意非 Noether 环 A 作为 A-模只有生成元 1，却不是 Noether 模；“有限生成”本身不控制所有子模。

## 核心定理与证明机制

模 Noether 当且仅当每个子模有限生成。若有非有限生成子模，依次选不在旧生成子模中的元素得到严格升链。反向，对任意升链取并集，它是子模；有限个生成元都在某一共同阶段，故链从此稳定。Hilbert 基定理：R Noether⇒R[x] Noether，此处借用，证明机制为首项系数理想及有限次降阶。

## 一个完整 Worked Example

证明 Z[x,y] 的每个理想有限生成。先由 Z 是 PID 得 Noether，应用一次 Hilbert 基定理得 Z[x] Noether，再一次得 Z[x,y] Noether。于是定义中的任意理想都有限生成。结论并没有说主理想：例如 (x,y) 不能由单个多项式生成，评价 x=y=0 可用于验证这个区别。

## 一个贯穿模型

k[x₁,x₂,…] 中 (x₁)⊊(x₁,x₂)⊊… 永不停止；k[x₁,…,x_n] 在有限 n 时却有理想有限生成的结论。

## 补充计算与条件辨析

判断 Z 作为 Z-模是否 Noether、Artin；给一个有限生成但底环非 Noether 导致的非 Noether 模。

Z Noether 非Artin，下降链(2ⁿ)；非Noether环 A 自身为一生成 A-模但非Noether。

证明等价的两方向；指出从“每个理想主生成”推广到有限生成时保留了什么。

无限构造生成元会给严格升链；链的并若有限生成，生成元落在同一有限阶段。

从 A Noether 证明 S⁻¹A Noether；对无限变量环写一条严格理想升链。

理想是其收缩理想的扩张，收缩有限生成；无限变量逐次加入新变量。

AI 草稿：“A-模 M 有限生成，所以 M 的每个子模都有限生成。”

需要 A Noether 等额外条件；取非Noether环 M=A 反驳。

证明 Z[x,y] 的每个理想有限生成，明确引用 Hilbert 基定理的层数；说明不保证主生成。

Z Noether，连续两次 Hilbert 基定理；(x,y) 一般非主。

## 练习与讨论

这些练习可与正文例解交叉使用；下面的梯度只是内容顺序。

### 看懂

1. 用自己的话解释本节的一个新符号，写清它的对象类型，并在最简单模型中指出对应对象。

### 模仿

- 判断 Z 作为 Z-模是否 Noether、Artin；给一个有限生成但底环非 Noether 导致的非 Noether 模。

### 独立使用

- 证明等价的两方向；指出从“每个理想主生成”推广到有限生成时保留了什么。
- 从 A Noether 证明 S⁻¹A Noether；对无限变量环写一条严格理想升链。

### 迁移

- AI 草稿：“A-模 M 有限生成，所以 M 的每个子模都有限生成。”
- 证明 Z[x,y] 的每个理想有限生成，明确引用 Hilbert 基定理的层数；说明不保证主生成。

## 与 ACTIONS 的连接

[生成](../maps/ACTIONS.md#m03)、[近似与滤过](../maps/ACTIONS.md#m15)。用本节的构造回看这些动作：输入是什么、允许什么操作、哪条关系或假设保证输出正确。

## 与 THEMES 的连接

[生成、关系与高阶关系](../maps/THEMES.md#t01)、[局部化、拼接与完备化](../maps/THEMES.md#t04)。地图将同一机制在后续对象中的变化并列展示，不要求现在掌握远端语言。

## 向现代研究延伸

[Higher Algebra](../maps/RESEARCH.md#r1)；可从 [HA00](../research/HA00.md) 的具体模型进入。先按该入口列出的先修补齐工具，再追问本节哪些信息在高阶构造中仍应保留。

## 查阅

以下是原课程保留的主题定位；未在本次逐页核对教材章号。

- [FER](../SOURCES.md#fer) · Ch.2
